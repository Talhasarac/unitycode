---
description: Build or revise a Unity UI Toolkit interface and verify it visually
agent: unity
---

Load both `unity-mcp-workflow` and `unity-ui-toolkit`. Implement this UI request: $ARGUMENTS

Discover existing UI assets and distinguish runtime from Editor UI. Use `manage_ui` and Unity-aware script/asset tools. Preserve the project's conventions. Exercise relevant interaction states, wait for clean compilation, inspect the console, run focused tests if present, save intended non-scene assets, and complete the screenshot-adjust-screenshot loop at compact and wide sizes where possible. Do not save or mutate a `.unity` scene unless the user explicitly requested that scene change.
